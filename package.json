{
  "name": "Orbital",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build:rust": "cd engine && wasm-pack build --target web --out-dir ../UI/engine",
    "dev": "npm run build:rust && vite",
    "build": "npm run build:rust && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.6"
  },
  "dependencies": {
    "three": "^0.168.0",
    "tweakpane": "^4.0.4"
  }
}
